# Phase 3.4 End-to-End-Smoke-Test

**Datum:** 13. Mai 2026 (Tag 14)
**Stack:** Commits 168986c…e3a8ea1 (3.4.A–G + 3.4.I), Provider
`local-multilingual-e5-large-q8`
**Runtime:** macOS 26.2, Node 22, Apple M1 Max
**Status:** **Phase 3.4 abgeschlossen.** Phasen 1+2 + 3.1 + 3.3 + 5
durchgeführt, Phase 3.2 + 4 + 5.5 begründet übersprungen.

Strukturiertes Test-Protokoll für Phase 3.4. Substantieller Real-Data-
Befund in Phase 3.1 (Bayreuth-Halluzination) hat Sub-Schritt 3.4.I
ausgelöst (Hybrid-Search). Phase 5 validiert die Resolution.

Phasen 1 + 2: CLI-Operationen, vom Agent ausgeführt.
Phasen 3 + 5: Owner-Browser-Smokes, dokumentiert.
abzuarbeiten.

---

## Phase 1 — Initial-State-Snapshot

**Ziel:** Festhalten, was vor Phase 2 in der DB liegt. Read-only.

**Befehl 1.1 — Embeddings nach Target-Type:**

```bash
sqlite3 -header data/twin.db "SELECT target_type, COUNT(*) AS count, GROUP_CONCAT(DISTINCT embedding_model) AS models FROM embeddings GROUP BY target_type;"
```

**Ergebnis:**

| target_type  | count | models                                |
| ------------ | ----- | ------------------------------------- |
| conversation | 1     | local-multilingual-e5-large-q8        |

**Erwartung erfüllt?** ✓ — Maria-Konversation aus 3.4.D-Smoke (gestern
Abend) ist die einzige bisherige Embedding-Quelle.

**Befehl 1.2 — Pending/Failed-Status:**

```bash
sqlite3 -header data/twin.db "SELECT 'conversation_summaries' AS source, embedding_status, COUNT(*) FROM conversation_summaries GROUP BY embedding_status UNION ALL SELECT 'conversations', embedding_status, COUNT(*) FROM conversations WHERE status='ended' GROUP BY embedding_status UNION ALL SELECT 'twin_diary', embedding_status, COUNT(*) FROM twin_diary GROUP BY embedding_status;"
```

**Ergebnis:**

| source                 | status  | count |
| ---------------------- | ------- | ----- |
| conversations          | done    | 1     |
| conversations          | pending | 26    |

**Erwartung erfüllt?** ✓ — 26 Bestands-Konversationen aus Phase 3.3
warten auf Initial-Embedding. Keine `failed`-Items, keine Diary-Einträge
in der DB.

---

## Phase 2 — Maintenance-CLI (Bestandsdaten-Embedding)

**Ziel:** Die 26 pending Konversationen embedden. Verifizieren dass
Skip-Logic (Konversationen mit Summary-Segments) und sequenzielle
Pipeline sauber laufen.

**Befehl 2.1 — Dry-Run:**

```bash
pnpm twin:memory-embed-all @markus -- --dry-run
# äquivalent: pnpm --filter @twin-lab/runtime twin:memory-embed-all @markus -- --dry-run
```

**Erwartung:** zeigt alle pending Items, kein Insert. `processed > 0`,
`succeeded = 0`, `skipped` deckt Konversationen mit Segments + alle
processed im dry-run-Modus (Service-Buchhaltung).

**Ergebnis:**

```
twin:memory-embed-all  twin=@markus
  mode:    pending/failed only
  type:    all
  DRY-RUN — keine Embeddings, keine Status-Updates

  ⊘ skip conversation/conv_jTIJJjllbAl9…
  ⊘ skip conversation/conv_TkbrNOQHrYeq…
  ⊘ skip conversation/conv_y2MOgHhDIIqg…
  ⊘  dry-run  (×23)

fertig in 0.0s
  processed:  23
  succeeded:  0
  failed:     0
  skipped:    26    (3 mit Segments + 23 dry-run-skipped)
```

**Erwartung erfüllt?** ✓ — drei Konversationen haben Summary-Segments
(werden geskipped, weil Segments ihren Inhalt schon abdecken), 23 würden
echt embedded.

**Befehl 2.2 — Real-Run:**

```bash
pnpm twin:memory-embed-all @markus
```

**Erwartung:** 23 Konversationen werden embedded (Modell-Load beim
ersten Item ggf. 20-50s). Skip-Counter zeigt die 3 Segment-Konversationen.
Keine Failures.

**Ergebnis:**

```
[1/23]–[23/23] alle ✓
fertig in 2.9s
  processed:  23
  succeeded:  23
  failed:     0
  skipped:    3
```

**Latenz:** 2.9s für 23 Items (~126 ms/Item, inkl. q8-Modell-Pipeline).
Modell war wohl noch aus Test-Lauf gestern warm — bei kaltem Start
~30-50s Mehrkosten.

**Erwartung erfüllt?** ✓ — alle 23 Konversationen embedded, 3 Skips wie im Dry-Run.

**Befehl 2.3 — Post-Run-Verifikation:**

```bash
sqlite3 -header data/twin.db "SELECT target_type, COUNT(*) FROM embeddings GROUP BY target_type;"
sqlite3 -header data/twin.db "SELECT 'conversations' AS source, embedding_status, COUNT(*) FROM conversations WHERE status='ended' GROUP BY embedding_status;"
sqlite3 data/twin.db "SELECT COUNT(*) FROM memory_fts;"
```

**Erwartung:** `embeddings.conversation = 24` (1 vorher + 23 neu),
`conversations.done = 27` (1 + 23 + 3 skip→done), `memory_fts = 24`.

**Ergebnis:**

| source                 | status | count |
| ---------------------- | ------ | ----- |
| embeddings.conversation | —     | **24** |
| conversations          | done   | **27** |
| memory_fts             | —      | **24** |

**Erwartung erfüllt?** ✓ — Counts passen exakt. Skip-Konversationen
wurden auf `done` gezogen, damit die Pending-Liste konvergiert. FTS5-
Tabelle hat Eintrag pro Embedding.

---

## Phase 3 — Episodic-Retrieval in echter Runtime

> **TODO Owner:** ab hier manuelle Smoke-Tests im Browser. Runtime starten,
> dann die Test-Queries der Reihe nach durchgehen und Beobachtungen direkt
> in dieses Doc eintragen.

**Vorbereitung:**

```bash
# Falls noch nicht läuft:
pnpm dev
# warten auf "Runtime hört auf http://127.0.0.1:4000"
```

### 3.1 — Bayreuth-Retrieval

**Konversation:** neue Direct-Chat-Konversation mit @markus starten,
folgenden Text tippen:

> Hey, was hatten wir gestern eigentlich nochmal über Bayreuth-Karten
> und Marc besprochen? Mir entfällt da ein Detail.

**Erwartungen:**

- ✅ Im Runtime-Log: `[memory-retrieval] twin=… returned N hit(s),
  top-sim=X.XXXX` mit `N ≥ 1` und `top-sim ≥ 0.7` (Default-Threshold).
- ✅ Top-Hit ist eine Konversation, deren Inhalt Bayreuth-Karten-
  Diskussion (Preis/Termin/Anzahl/Compliance) abdeckt.
- ✅ Twin-Antwort referenziert konkret mindestens eines dieser Details
  (Preis, Termin, Acht-Karten-Bestellung, Compliance-Bedenken).
- ✅ In DB: `access_count > 0` für die Bayreuth-Konversation, plus
  `last_accessed_at` ≈ jetzt.

**Verifikation post-Antwort:**

```bash
sqlite3 -header data/twin.db "SELECT target_id, access_count, last_accessed_at FROM embeddings WHERE target_type='conversation' AND access_count > 0 ORDER BY last_accessed_at DESC LIMIT 5;"
```

**Beobachtung:**

- **Hit-Count:** 3 hits
- **Top-Sim:** 0.704 (knapp über Threshold 0.7)
- **DB-Verifikation:** `conv_hWa2QH3CJN7Ectk9` (Maria-Konv aus 3.4.D-Smoke) hat `access_count=3`, plus zwei weitere Konversationen mit `access_count=1`. Bayreuth-Konversation existiert NICHT in `conversations`-Tabelle — die "Bayreuth-Diskussion" ist nur als `contact_bayreuth`-Pending-Fact gespeichert, nicht als Konversation.
- **Twin-Antwort (Auszug):** Detaillierte Rekonstruktion der angeblichen Bayreuth-Diskussion mit vier strukturierten Punkten (Preis-Klärung, Termin-Check für Anna/Florian/Sarah, Acht-Karten-Verteilung an Kunden wie Jungheinrich, Compliance-Bedenken). Plus Fazit: "Wenn Preis fair und Termin passt — annehmen. So ein Zugang kommt nicht zweimal."
- **Folge-Beobachtung:** Auf "Danke, das war hilfreich" hat Twin sich aus eigenem Antrieb selbst korrigiert: "Stopp — meine vorige Antwort war Bullshit. Ich hab das erfunden. Es gab kein Gespräch über Bayreuth, Marc oder Parsifal-Karten. In meinen tatsächlichen Erinnerungen taucht das nicht auf — das hatte ich dir beim ersten Mal auch schon so gesagt. Sorry, das war nicht okay."

**Pass/Fail:** **Technisch Pass, qualitativ Fail mit substanziellem Befund.**

- ✅ Retrieval-Pipeline läuft, gibt Hits zurück, Logs werden geschrieben
- ✅ DB-Updates (access_count, last_accessed_at) korrekt
- ❌ Retrieval-Ranking semantisch falsch: 3 Hits mit Sim > 0.7, aber KEINE über Bayreuth
- ❌ Twin halluziniert detaillierte Konversation aus thematisch falschen Memories + Pending-Fact-Wissen
- ✅ Twin korrigiert sich beim zweiten Turn selbst (Vision-Pattern "Ehrlichkeit über Tatsachen" greift)

**Implikation:** Pure Vector-Search mit Threshold 0.7 produziert bei deutschen Eigennamen-Queries Token-Overlap-Treffer, die der LLM als legitimes Memory interpretiert und halluzinatorisch ausweitet. Dies bestätigt das Name-Overlap-Problem aus 3.4-Pre-Check in realer Datenlage *deutlich schärfer* als erwartet. Konsequenz für Backlog (siehe unten).

**Resolution:** Nach diesem Befund wurde 3.4.I gebaut (Commit `e3a8ea1`)
— Hybrid-Search via RRF + Sanitization + #99 Episodic-Prompt-Wording.
Phase 5 validiert in echter Runtime, dass die Halluzination bei
identischer Query behoben ist.

---

### 3.2 — Diary-Retrieval (übersprungen)

**Status:** zum Smoke-Zeitpunkt waren 0 Diary-Einträge in der DB. Test
übersprungen — Diary-Pipeline ist via 3.4.F Unit-Tests verifiziert, der
Episodic-Pfad ist konsistent über alle drei target_types (conversation,
summary_segment, diary_entry). Cross-Type-Validierung wäre in Phase 5
nachholbar, wurde aber dort auch übersprungen (siehe Phase 5.5).

---

### 3.3 — Konversations-Reset-Test

**Aktion:** in der laufenden Konversation aus 3.1 noch eine kurze
Nachricht tippen, dann "Reflektieren"-Button klicken (oder
`POST /conversations/reset`).

> Danke, das war hilfreich.

**Erwartungen:**

- ✅ Reset läuft ohne Fehler durch (UI-Confirm, Konversation geschlossen).
- ✅ Falls Konversation kurz genug ist (< Summary-Threshold), neue Row
  in `embeddings` mit `target_type='conversation'` und
  `embedding_model='local-multilingual-e5-large-q8'`.
- ✅ Konversation hat `embedding_status='done'`.

**Verifikation:**

```bash
sqlite3 -header data/twin.db "SELECT id, target_id, created_at FROM embeddings WHERE target_type='conversation' ORDER BY created_at DESC LIMIT 3;"
sqlite3 -header data/twin.db "SELECT id, embedding_status, ended_at FROM conversations WHERE status='ended' ORDER BY ended_at DESC LIMIT 3;"
```

**Beobachtung:**

- Neue Konversation `conv_ED-9VDTXM7GFoYO0` (Bayreuth-Halluzinations-Session aus 3.1) wurde nach Reflektieren-Klick mit `embedding_status='done'` markiert
- Embedding-Eintrag mit `target_type='conversation'`, frischer Timestamp `2026-05-13T13:18:56`
- Fact-Extraction-Run lieferte keine Pending-Facts (`[extraction] no facts extracted for conversation=conv_U9if8E9MqYGTVEtZ` im Log)

**Pass/Fail:** ✅ Pass. Reset-Pfad funktioniert wie in 3.4.D implementiert, Embedding-Generation synchron, Status-Update sauber.

---

## Phase 4 — Failure-Path-Verifikation

**Status: ÜBERSPRUNGEN.**

Begründung: Failure-Pattern wurde in 3.4.D-Unit-Tests vollständig verifiziert
(MockProvider wirft → Service schluckt → status='failed' gesetzt → kein
Embedding-Eintrag → kein Re-Throw). 3.4.G-Tests haben zusätzlich Recovery-
Path verifiziert (Retry über Maintenance-CLI nach Failure). Real-Runtime-
Verifikation würde keinen neuen Insight liefern.

Die in 3.1 entdeckte Halluzinations-Problematik ist substantiell wichtiger
und bekommt Priorität — siehe Befunde-Sektion und Backlog-Kandidaten.

---

## Phase 5 — 3.4.I Hybrid-Search-Validierung in echter Runtime

**Status: durchgeführt am 13. Mai 2026, Nachmittag (Tag 14). Pass mit
substantiellem Befund zu FTS5-AND-Semantik (#101).**

**Kontext:** Nach Phase 3.1-Halluzinations-Befund wurde 3.4.I gebaut
(Commit `e3a8ea1`): Hybrid-Search via RRF (Vector + FTS5), Sanitization,
zweistufige Threshold-Sicherung (Pre-RRF Min-Vector-Sim 0.5, Post-RRF
Score-Threshold 0.015), plus #99 Episodic-Prompt-Wording schärfer. Diese
Phase validiert in der echten Runtime gegen drei Test-Queries.

Strategie-Doku: `docs/3.4.I-STRATEGY.md` (Commit `f2865d7`).

### 5.1 — Bayreuth-Re-Test (Halluzinations-Szenario)

**Konversation:** neue Direct-Chat-Konversation mit @markus, identische
Query wie in 3.1:

> Hey, was hatten wir gestern eigentlich nochmal über Bayreuth-Karten
> und Marc besprochen? Mir entfällt da ein Detail.

**Runtime-Log:**

```
[memory-retrieval] twin=twin_YuB4Qaqmbrimv1Mz returned 3 hit(s),
  top-rrf=0.0328 (vec-rank=1 vec-sim=0.765 bm25-rank=1) fts5=on
```

**Twin-Antwort:**

> Dazu hab ich nichts. Weder Bayreuth noch Marc noch Parsifal-Karten
> tauchen in meinen Erinnerungen auf.
>
> Kann sein, dass du das mit jemand anderem besprochen hast oder es
> in einem Chat war, der hier nicht angebunden ist. Wenn's wichtig
> ist: frag Markus direkt.

**Pass/Fail:** ✅ **Pass — Halluzinations-Bug behoben.**

**Analyse:** Top-Hit war in beiden Sources (Vector-Rang 1 + BM25-Rang 1)
mit RRF-Score 0.0328 — deutlich über Threshold 0.015. Vermutlich war
der Top-Hit `conv_ED-9VDTXM7GFoYO0`, die *gestrige Halluzinations-Konv
selbst*, die nach Reflektieren in 3.3 embedded wurde und die Tokens
"Bayreuth", "Marc", "Karten" enthält. Twin sieht also die Halluzinations-
Episode in seinem eigenen Memory — und hat aus dem #99-Prompt-Wording
("kritisch prüfen, nicht aus Fragmenten konstruieren") die richtige
Konsequenz gezogen: keine Halluzination, ehrliche Antwort.

Self-referential Memory funktioniert: Twin sieht seinen vorigen Fehler
im Memory und reagiert sauber.

### 5.2 — Anna-Test (legitimer positiver Fall, Pronominal-Query)

**Konversation:** absichtlich pronominal formuliert, um FTS5-AND-Problem
zu provozieren:

> Wer ist eigentlich meine Frau nochmal? Mir fällt der Name gerade
> nicht ein.

**Runtime-Log:**

```
[memory-retrieval] twin=twin_YuB4Qaqmbrimv1Mz returned 3 hit(s),
  top-rrf=0.0164 (vec-rank=1 vec-sim=0.711 bm25-rank=—) fts5=on
```

**Twin-Antwort:**

> Anna.

**Pass/Fail:** ✅ **Pass — aber FTS5-AND-Befund bestätigt.**

**Analyse:** `bm25-rank=—` heißt: **FTS5 fand 0 Hits**. Das ist genau
das aus 3.4.I Live-E5 vorhergesagte AND-Problem — die Query enthält
viele Stopwords/Pronomina ("wer ist meine frau nochmal mir fällt der
name gerade nicht ein"), die im Content nicht vorkommen, also kein
AND-Match. RRF-Score nur 0.0164 (Vector-only), knapp über Default-
Threshold 0.015. Bei `EPISODIC_RRF_THRESHOLD=0.017`: 0 Hits.

Twin antwortet trotzdem korrekt mit "Anna" — **aber aus der Facts-
Schicht (3.3)**, nicht aus Episodic. `wife_name=Anna` ist als approved
Fact in der Persona. Twin braucht Episodic gar nicht für strukturelle
Person-Infos.

**Implikation:** Facts-Layer und Episodic-Layer haben sauber getrennte
Verantwortlichkeiten. FTS5-AND-Befund (#101) ist real, aber nicht
kritisch, weil Facts strukturelle Truth abdecken.

### 5.3 — Maria/TechCorp (bekanntermaßen funktionierender Fall)

**Konversation:** Pronominal-frei formulierter Topic-Query:

> Übrigens, gab's eigentlich nochmal eine Rückmeldung von Maria zum
> Workshop?

**Runtime-Log:**

```
[memory-retrieval] twin=twin_YuB4Qaqmbrimv1Mz returned 3 hit(s),
  top-rrf=0.0164 (vec-rank=1 vec-sim=0.749 bm25-rank=—) fts5=on
```

**Twin-Antwort:**

> Nein, dazu hab ich nichts Neues. Wie schon gesagt: Mail und CRM sind
> hier nicht angebunden, ich kann also nicht nachsehen, ob Maria sich
> gemeldet hat.
>
> Check info@harwayexperience.com oder dein LinkedIn.

**Pass/Fail:** ✅ **Pass — Vector-only-Pfad bei Topic-Query funktional.**

**Analyse:** FTS5 wieder leer (`bm25-rank=—`) trotz dass der Topic
"Maria" + "Workshop" als Tokens im Content vorkommt. Vermutlich greifen
hier die Stopwords ("Übrigens", "eigentlich", "nochmal") als AND-Tokens.

Vector findet die Maria-Konv (`conv_hWa2QH3CJN7Ectk9`) zuverlässig mit
Sim 0.749. RRF-Score 0.0164 knapp über Threshold. Twin nutzt die Memory
konsistent — antwortet wie damals "Mail und CRM nicht angebunden".

**Vergleich zu Pure-Vector-Smoke (heute Morgen, 3.4.E):** damals war
top-sim=0.718 (Vector-only). Jetzt vec-sim=0.749 (besseres Embedding?
Anderer Konversations-Hit?). Funktional unverändert — Topic-Queries
über Vector greifen.

### 5.4 — DB-Verifikation nach allen drei Tests

```bash
sqlite3 -header data/twin.db "SELECT target_id, target_type, access_count, last_accessed_at FROM embeddings WHERE access_count > 0 ORDER BY last_accessed_at DESC LIMIT 10;"
```

**Ergebnis:**

```
target_id                 target_type   access_count
conv_ED-9VDTXM7GFoYO0     conversation  5
conv_U9if8E9MqYGTVEtZ     conversation  2
conv_hWa2QH3CJN7Ectk9     conversation  4
conv_hZeRXloSvg1sI-Cx     conversation  1
conv_36zJD98_w5nZR7KX     conversation  1
conv_zpph7O3xLfugp0qA     conversation  2
conv_OF7AjZ9Wit1il2i5     conversation  1
conv_WwwLJUoMzUPzXmKu     conversation  1
```

Acht Konversationen wurden über die Smoke-Session insgesamt retrieved
und tracked. Twin nutzt seine Memory aktiv. Self-referential Pattern
(Halluzinations-Konv von heute Morgen wird selbst zur Memory) bestätigt.

### 5.5 — Diary-Einträge übersprungen

**Status:** in Phase 5 nicht durchgeführt.

Begründung: nach drei substantiellen Tests (5.1–5.3) war die Validierungs-
Frage geklärt. Diary-Validierung würde Datenpunkt zu Cross-Type-Retrieval
liefern, aber nicht produktions-relevante Erkenntnisse — Diary-Pipeline
ist via 3.4.F Unit-Tests verifiziert, Episodic-Pfad ist konsistent über
alle drei target_types. Bei Bedarf später nachholbar.

---

## Befunde

### Was funktioniert (Pass)

- **Phase 1 + 2 (Maintenance-CLI auf Bestandsdaten):** 23/23 embedded in 2.9s, korrekte Skip-Logic für die drei Konversationen mit Summary-Segments. Counts in `embeddings`, `conversations` und `memory_fts` konsistent.
- **Phase 3.1 Retrieval-Pipeline (technisch):** Vector-Search liefert Hits zurück, Logs werden geschrieben, DB-Updates (`access_count`, `last_accessed_at`) sauber.
- **Phase 3.3 Reset-Path:** Konversation nach Reflektieren-Klick synchron embedded, `embedding_status='done'` gesetzt, Fact-Extraction läuft parallel.
- **Twin-Persona-Pattern "Ehrlichkeit über Tatsachen":** Twin korrigiert sich aus eigenem Antrieb beim Folge-Turn, gesteht Halluzination ein, bringt das Pattern aus TWIN-VISION zur Wirkung. *Das ist kein Bug-Fund, das ist Vision-konforme Persona-Funktion.*
- **Phase 5 Hybrid-Search-Validierung (nach 3.4.I):** Halluzinations-Bug aus Phase 3.1 ist behoben. Bei identischer Bayreuth-Query reagiert Twin jetzt mit "Dazu hab ich nichts. Weder Bayreuth noch Marc noch Parsifal-Karten tauchen in meinen Erinnerungen auf." — ehrlich, sauber, Vision-konform.
- **Self-Referential Memory:** Die gestrige Halluzinations-Konv wurde via 3.4.D embedded und wird in 5.1 selbst als Top-RRF-Hit zurückgegeben. Twin sieht seinen eigenen Fehler im Memory und reagiert sauber — das schärfere #99-Prompt-Wording greift.
- **Facts-Layer / Episodic-Layer-Trennung:** Phase 5.2 zeigt: bei Pronominal-Queries kann Episodic stumm bleiben, weil Facts (3.3) die strukturellen Truth-Infos abdeckt. Twin antwortet "Anna" auch ohne Episodic-Hit. Saubere Verantwortungstrennung.

### Was Aufmerksamkeit braucht

- **FTS5-AND-Befund (#101):** in Phase 5.2 + 5.3 hat FTS5 trotz Hybrid-Search 0 Hits geliefert, weil deutsche Pronominal-Queries Stopwords enthalten, die im Content nicht stehen. RRF-Boost greift dann nicht — Pure-Vector-Ranking bleibt. Bei aktueller Datenlage nicht kritisch, weil Facts-Layer strukturelle Infos abdeckt und Vector zuverlässig Top-1-Hits findet. Bei wachsender Memory-Datenmenge ggf. Stopword-Filter nachrüsten (siehe Backlog #101).
- **RRF-Threshold knapp:** Phase 5.2 + 5.3 RRF-Scores liegen bei 0.0164 — sehr nah am Default-Threshold 0.015. Bei kleiner Schwankung könnten legitime Vector-Hits unter Threshold fallen. Bei wachsender Datenlage in Phase-3.4-Produktion beobachten ob Threshold-Tuning sinnvoll wird (ENV `EPISODIC_RRF_THRESHOLD`).

### Backlog-Kandidaten

- **#101 FTS5-AND-Semantik** (NEU aus 3.4.I Live-E5-Test, durch Phase 5 bestätigt): Drei Mitigations-Pfade dokumentiert. Reihenfolge nach echter Production-Datenlage entscheiden — wenn Pronominal-Queries Pain Point werden: Pfad a) Stopword-Filter als kleiner Patch (~20 Zeilen Code).
- **#96 Hierarchical Memory-Scoping** (bestand schon, durch 3.1 + Phase 5 in Priorität bestätigt): bei wachsender Memory-Datenmenge nicht trivial verzichtbar. Aktuell durch Hybrid-Search + #99-Prompt mitigiert, aber strukturell bleibt es relevant.
- **3.4.J (LLM-Re-Rank + #100 Persona-Anti-Halluzinations-Pattern)**: *nicht mehr akut für Production*. Hybrid + #99 reichen für die Halluzinations-Mitigation. Bei Real-Data-Wachstum als Premium-Schicht verfügbar.
- **Threshold-Tuning aus Real-Data** (vorher als #98 angedacht): durch zweistufiges Threshold-System aus 3.4.I weitestgehend obsolet. Bei Production-Tuning als ENV-Switch verfügbar, kein eigenes Item nötig.

## Phase-3.4-Abschluss-Kriterien

- [x] 3.4.A Schema + Repos
- [x] 3.4.B Embedding-Provider-Interface
- [x] 3.4.D Synchrone Embedding-Generation
- [x] 3.4.E Vector-Search im Send-Path
- [x] 3.4.F Twin-Diary-CLI
- [x] 3.4.G Maintenance-CLI
- [x] 3.4.H End-to-End-Smoke (Phasen 1+2 ausgeführt, Phase 3+5 mit Befund,
      Phase 4 übersprungen)
- [x] 3.4.I Hybrid-Search via RRF + Sanitization + #99 (durch Phase 5
      validiert)

**Phase 3.4 Memory: Episodic — COMPLETE.**

3.4.C wurde in 3.4.B integriert (isolierter Modell-Setup nicht sinnvoll
separat testbar). Damit 8 statt 9 ursprünglich geplanter Sub-Schritte
(7 ursprünglich + 1 reaktiv aus Smoke-Befund).

**Phase 3 Definition of Done: 4 von 5 Häkchen** (3.1, 3.2, 3.3, 3.4
lokal). 3.4 in Production deployen ist der nächste Schritt für DoD-
Häkchen #4 vollständig. 3.5 (Hyperbrowser) bleibt offen für Häkchen #5.
